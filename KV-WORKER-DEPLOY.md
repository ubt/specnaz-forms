# Инструкция по развертыванию KV Worker

## 📋 Описание

Исправленный Cloudflare Worker для обработки очереди операций обновления Notion.

**Исправлена проблема:** "Too many subrequests" при обработке больших батчей.

**Решение:** Worker теперь контролирует общее количество операций за один запуск и никогда не превышает лимит Cloudflare Workers (50 subrequests).

## 🚀 Развертывание

### 1. Создание Worker в Cloudflare Dashboard

1. Перейдите в [Cloudflare Dashboard](https://dash.cloudflare.com/) → Workers & Pages
2. Нажмите **Create Worker**
3. Дайте имя: `notion-queue-worker` (или любое другое)
4. Нажмите **Deploy**

### 2. Замена кода Worker

1. В созданном worker'е нажмите **Quick Edit**
2. Удалите весь код
3. Скопируйте весь код из файла `kv-worker-fixed.js`
4. Вставьте в редактор
5. Нажмите **Save and Deploy**

### 3. Настройка переменных окружения

В настройках Worker → **Settings** → **Variables**:

#### Обязательные:
- `NOTION_TOKEN` - Ваш Notion Integration Token
- `NOTION_QUEUE_KV` - KV Namespace binding (см. ниже)

#### Опциональные (с рекомендуемыми значениями):
- `SAFE_SUBREQUEST_LIMIT` = `40` (безопасный лимит операций за запуск)
- `MAX_JOBS_PER_RUN` = `5` (макс. jobs за один запуск)
- `CONCURRENCY` = `1` (параллельность обработки)
- `RATE_LIMIT_MS` = `2000` (задержка между операциями в мс)
- `MAX_RETRIES` = `3` (количество повторов при ошибках)
- `NOTION_VERSION` = `2022-06-28` (версия Notion API)
- `DRAIN_TOKEN` - Токен для защиты endpoint `/drain` (опционально)

### 4. Настройка KV Namespace Binding

1. В настройках Worker → **Settings** → **Bindings**
2. Нажмите **Add Binding**
3. Выберите **KV Namespace**
4. Variable name: `NOTION_QUEUE_KV`
5. KV namespace: выберите ваш существующий `NOTION_QUEUE_KV` namespace
6. Нажмите **Save**

### 5. Настройка Cron Trigger

1. В настройках Worker → **Triggers** → **Cron Triggers**
2. Нажмите **Add Cron Trigger**
3. Введите расписание:
   - `*/3 * * * *` - каждые 3 минуты (рекомендуется)
   - `*/5 * * * *` - каждые 5 минут (менее частая обработка)
   - `*/1 * * * *` - каждую минуту (быстрая обработка, но больше нагрузка)
4. Нажмите **Add Trigger**

## 🧪 Тестирование

### Ручной запуск обработки:

```bash
# Без авторизации (если DRAIN_TOKEN не установлен)
curl https://notion-queue-worker.yourname.workers.dev/drain

# С авторизацией (если DRAIN_TOKEN установлен)
curl -H "Authorization: Bearer YOUR_DRAIN_TOKEN" \
     https://notion-queue-worker.yourname.workers.dev/drain
```

### Проверка здоровья:

```bash
curl https://notion-queue-worker.yourname.workers.dev/health
```

### Проверка Notion токена:

```bash
curl -H "Authorization: Bearer YOUR_DRAIN_TOKEN" \
     https://notion-queue-worker.yourname.workers.dev/self-test
```

## 📊 Что изменилось

### Старая логика (с ошибкой):
```
Cron запуск
├─ Job1: 50 операций → 50 subrequests
├─ Job2: 20 операций → 20 subrequests
└─ ИТОГО: 70 subrequests → ❌ ОШИБКА "Too many subrequests"
```

### Новая логика (исправлено):
```
Cron запуск #1
├─ Job1: 40 из 50 операций → 40 subrequests ✅
└─ Job1 обновлён: 10 операций осталось

Cron запуск #2
├─ Job1: 10 оставшихся операций → 10 subrequests ✅
└─ Job2: 20 операций → 20 subrequests ✅
└─ ИТОГО: 30 subrequests ✅ Всё ОК
```

## 🔧 Как это работает

1. **Worker запускается по cron** (каждые 3 минуты)
2. **Читает до 5 pending jobs** из KV
3. **Считает доступные "слоты"**: `40 - уже_обработано`
4. **Обрабатывает операции** пока не достигнет лимита 40
5. **Частично обработанные jobs** сохраняются обратно в KV
6. **Следующий запуск** продолжает с того места, где остановился

## ⚙️ Рекомендуемые настройки config.js

В файле `lib/config.js` вашего Next.js приложения:

```javascript
BATCH: {
  DEFAULT_BATCH_SIZE: 20,  // ← Оптимально для worker'а
  MAX_BATCH_SIZE: 40,      // ← Безопасно для worker'а
  MAX_CONCURRENCY: 1,
  MIN_RATE_LIMIT_DELAY: 2000,
  MAX_RETRIES: 3,
}
```

**Почему 20-40?**
- Worker обрабатывает максимум 40 операций за запуск
- Job с 20 операциями будет обработан за один запуск
- Job с 50 операциями будет разбит: 40 в первый запуск, 10 во второй

## 📈 Мониторинг

### Логи Worker'а:

1. Cloudflare Dashboard → Workers → notion-queue-worker
2. Вкладка **Logs** → **Begin log stream**

### Типичные логи успешной обработки:

```
[DRAIN] Starting drain cycle. Safe limit: 40 ops
[DRAIN] Found 2 jobs, processing up to 2
[DRAIN] Job job_batch_xxx_0: 50 total ops, processing 40, 10 will remain
[DRAIN] Job job_batch_xxx_0: PARTIAL completion. Processed 40/50 ops
[DRAIN] Drain cycle completed: {
  "processedJobs": 1,
  "partialJobs": 1,
  "totalOpsProcessed": 40,
  "safeLimit": 40,
  "utilizationPercent": 100
}
```

## 🐛 Troubleshooting

### Worker не запускается по cron:
- Проверьте, что Cron Trigger настроен правильно
- Проверьте логи на наличие ошибок

### Ошибка "NOTION_TOKEN secret is missing":
- Добавьте переменную окружения `NOTION_TOKEN` в настройках Worker

### Ошибка "KV unavailable":
- Проверьте, что KV Namespace binding `NOTION_QUEUE_KV` настроен
- Проверьте, что выбран правильный KV namespace

### Jobs не обрабатываются:
- Проверьте логи Worker'а
- Вручную запустите `/drain` endpoint
- Проверьте, что jobs есть в KV (можно проверить через wrangler или KV UI)

## 📝 Коммит изменений

После успешного тестирования, закоммитьте изменения:

```bash
git add lib/config.js
git commit -m "Fix: уменьшить DEFAULT_BATCH_SIZE и MAX_BATCH_SIZE для безопасной обработки в worker"
git push origin <your-branch>
```

## ✅ Чеклист развертывания

- [ ] Worker создан в Cloudflare Dashboard
- [ ] Код из `kv-worker-fixed.js` развернут
- [ ] `NOTION_TOKEN` настроен
- [ ] `NOTION_QUEUE_KV` binding настроен
- [ ] `SAFE_SUBREQUEST_LIMIT=40` установлен
- [ ] Cron trigger настроен (*/3 * * * *)
- [ ] Тест через `/health` прошёл успешно
- [ ] Тест через `/drain` прошёл успешно
- [ ] `lib/config.js` обновлён (DEFAULT_BATCH_SIZE=20, MAX_BATCH_SIZE=40)
- [ ] Изменения закоммичены и запушены

## 🎉 Готово!

Теперь ошибка "Too many subrequests" не должна возникать!
