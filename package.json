{
  "name": "notion-review-cloudflare",
  "private": true,
  "scripts": {
    "build": "next build",
    "dev": "next dev",
    "start": "next start",
    "pages:build": "npx @cloudflare/next-on-pages",
    "pages:deploy": "npm run pages:build && wrangler pages publish .vercel/output/static"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15",
    "jose": "^5.2.0",
    "next": "^14.2.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/next-on-pages": "^1.13.0"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}